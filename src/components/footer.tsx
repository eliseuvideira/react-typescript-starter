import React from 'react';
import styled from 'styled-components';

const StyledFooter = styled.footer``;

const Footer = () => <StyledFooter>&copy; Made with React</StyledFooter>;

export default Footer;
